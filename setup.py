from setuptools import setup, find_packages

setup(
    name="fairness_pipeline_bank_marketing",
    version="0.1.0",
    author="Shay",
    packages=find_packages(),
    install_requires=[
        # Add your project dependencies here
    ],
)
